<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MeowTextReader.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MeowTextReader"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="MeowTextReader">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Margin="20" x:Name="RootGrid">
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <Button Content="返回上一頁" Click="Back_Click" Width="100" Margin="0,0,8,0"/>
                <Button Content="選擇資料夾" Click="SelectFolder_Click" Width="120" Margin="0,0,0,10"/>
            </StackPanel>
            <TextBlock Text="{Binding FolderPath, Mode=OneWay}" FontSize="14"/>
            <ScrollViewer Height="300" Margin="0,10,0,0">
                <ListView x:Name="FolderListView" ItemsSource="{Binding FolderItems}" SelectionMode="None" IsItemClickEnabled="True" ItemClick="FolderListView_ItemClick">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="Gray" BorderThickness="0,0,0,1" Margin="0,0,0,4" Padding="4,2">
                                <TextBlock Text="{Binding Name}" FontWeight="{Binding IsFolder, Converter={StaticResource FolderBoldConverter}}"/>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</Window>
