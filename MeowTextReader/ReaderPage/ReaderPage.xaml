<Page
    x:Class="MeowTextReader.ReaderPage.ReaderPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MeowTextReader.ReaderPage"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Content="返回" Width="80" Margin="0,0,0,10" Click="BackButton_Click"/>
            <TextBlock Text="{Binding FileName}" TextWrapping="Wrap" Margin="8,0,0,0"  VerticalAlignment="Center" TextAlignment="Start"/>
        </StackPanel>
        <ListView x:Name="FileListView" Grid.Row="1" ItemsSource="{Binding FileLines}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextWrapping="WrapWholeWords" IsTextSelectionEnabled="True" FontSize="20" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>